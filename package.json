{
  "name": "art-gobbler-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ art-gobbler-test",
    "create-local": "graph create --node http://localhost:8020/ art-gobbler-test",
    "remove-local": "graph remove --node http://localhost:8020/ art-gobbler-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 art-gobbler-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.2",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
